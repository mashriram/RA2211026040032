// frontend/components/UserProfile.js (same as before)
function UserProfile({ user }) {
    return (
      <div>
        <h2>{user.username}</h2>
      </div>
    );
  }

  export default UserProfile;